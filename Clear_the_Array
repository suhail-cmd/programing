#python
class Solution:
    # @param A : list of integers
    # @return an integer
    def solve(self, A):
        freq = {}
        for i in A:
            if i not in freq:
                freq[i] = 1
            else:
                freq[i] += 1
        return max(freq.values()) 
