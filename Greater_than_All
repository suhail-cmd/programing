class Solution:
    # @param A : list of integers
    # @return an integer
    def solve(self, A):
        max_val = float('-inf')
        count = 0
        for num in A:
            if num > max_val:
                count += 1
                max_val = num
        return count
            
