#python
class Solution:
	# @param A : integer
	# @return an integer
	def reverse(self, A):
        neg = A < 0
        A = abs(A)
        rev = int(str(A)[::-1])
        if neg:
            rev = -rev
        if rev < -2**31 or rev > 2 ** 31 -1:
            return 0
        return rev
